project-dir: plan-test
config: plan-rproject.toml

test:
  steps:
  - name: "rv init"
    run: "rv init"
    thread: rv
    assert: 
      contains: "rv project successfully initialized at ."
      not-contains: "this is not in stdout"
  
  - name: "rv plan"
    run: "rv plan"
    thread: rv
    insta: "snapshots/plan-test.snap"
 
