[project]
name = "configure-args-example"
r_version = "4.5"

repositories = [
    { alias = "cran", url = "https://cran.r-project.org" }
]

dependencies = [
    { name = "rgl", force_source = true},
]

[project.configure_args]
"rgl" = [
  { os = "macos", args = ["--disable-opengl"] },
]

[project.packages_env_vars]
"fs" = { USE_BUNDLED_LIBUV = "1" }