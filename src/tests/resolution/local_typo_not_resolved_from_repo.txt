[project]
name = "local-typo"
r_version = "4.4"
repositories = [
    {alias = "repo1", url = "http://repo1/"}
]
dependencies = [
    # typo in the local path: this must fail and must NOT be silently resolved
    # from repo1, which also serves rv.git.pkgA
    { name = "rv.git.pkgA", path = "typo-dir" },
    # depends on rv.git.pkgA (>= 0.0.5), which is available in repo1
    "rv.git.pkgD",
]
---
repos = [{name = "repo1", source = "test_repo1", force_source = false}]
---